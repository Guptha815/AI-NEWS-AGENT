fastapi → backend API framework

uvicorn → runs FastAPI server

requests → calling GNews API

transformers → HuggingFace summarization & sentiment models

torch → needed by transformers for model execution

streamlit → frontend chat UI
